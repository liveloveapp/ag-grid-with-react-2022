# React with Ag Grid Workshop

To get started, first ensure you are running the correct version of Node:

```sh
nvm use
```

Then install the dependencies:

```sh
npm install
```

Finally, start the dev server:

```sh
npm start
```
